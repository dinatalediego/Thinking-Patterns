[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "thinking-patterns"
version = "0.1.0"
description = "Metapatrones: OOP para diseño de pensamiento"
authors = [{name="Tu Nombre", email="tu.email@example.com"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Homepage = "https://github.com/tuusuario/thinking-patterns"
