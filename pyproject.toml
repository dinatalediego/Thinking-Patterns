[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "thinking-patterns"
version = "0.2.0"
description = "Metapatrones y Decision Intelligence determinística para pensamiento auditable"
authors = [{name="Diego Di Natale"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Homepage = "https://github.com/dinatalediego/Thinking-Patterns"
