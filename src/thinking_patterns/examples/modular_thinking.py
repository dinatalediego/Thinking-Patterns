from thinking_patterns.core.patterns_catalog import ModularizacionComoPensamiento

if __name__ == "__main__":
    p = ModularizacionComoPensamiento()
    print(p.aplicar({"dominio": "ETL de pricing"}))
